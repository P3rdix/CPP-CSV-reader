#ifndef CSV
#define CSV

#include <vector>

class Frame{
    struct column* a;
    
}

struct init{
    std::string name;
    vector<int>;
    vector<float>;
    vector<std::string>;
    struct column* next;
};

struct intcol{

};

struct floatcol{

};

struct strcol{

};

auto

#endif